export const renderRelationship = (relationship)=>{
    if (relationship === "LOVED_ONE") return "Loved-One"
    if (relationship === "NURSE") return "Nurse"
    if (relationship === "DOCTOR") return "Doctor"
}